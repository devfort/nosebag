import React from 'react';
import ReactDOM from 'react-dom';

import Search from './search';
import ShoppingList from './shopping_list';

let node = document.getElementById('content');
ReactDOM.render(<Search />, node);
